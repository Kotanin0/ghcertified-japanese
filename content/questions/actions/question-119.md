---
archetype: "questions"
title: "Question 119"
question: "action.ymlファイルを見てアクションがコンテナアクションであるかどうかを判断するにはどうすればよいですか？"
---


> https://docs.github.com/en/actions/creating-actions/metadata-syntax-for-github-actions#runs-for-docker-container-actions

1. [x] `runs.using`が`docker`という値を持つ
1. [ ] `runs.using`が`container`という値を持つ
1. [ ] `runs.using`が`Dockerfile`という値を持つ
1. [ ] `runs.main`が`container`という値を持つ
